---
import LogoImg from "../assets/logo_chp.webp";

const officeInfo = {
  address: "55/20 Soi Ronnasittipichai Sanambinnam Road Tasai Muang Nonthaburi 11000 Thailand",
  phone: "+662 580 2552 - 3",
  email: "contact@chpaisarn.com",
  email2: "natty@chpaisarn.com",
};

const factoryInfo = {
  address: "179 Moo11 Banchian Hunka Chainat 17130 Thailand",
  phone: "+665 645 1982 - 6",
};

const products = ["Body Armors", "Ballistic Helmets", "Ballistic Plates", "Other Ballistic Solutions", "Customized Defense Solutions"];

const company = ["About Us", "Quality", "Contact Us"];
---

<footer class="footer">
  <div class="footer-bar"></div>

  <div class="footer-inner">
    <div class="footer-grid">

      <!-- Logo & Copyright -->
      <div class="footer-brand">
        <img src={LogoImg.src} alt="Ch.Paisarn Logo" class="footer-logo" />
        <p class="footer-copy">© 2026 Ch.Paisarn. All Rights Reserved.</p>
        <p class="footer-copy">Website created by House of Bandan</p>
      </div>

      <!-- Office -->
      <div class="footer-col">
        <h3 class="footer-heading">Contact Us</h3>
        <div class="footer-contact-list">
          <div class="footer-contact-block">
            <p class="footer-label">Office</p>
            <p class="footer-text">{officeInfo.address}</p>
          </div>
          <div class="footer-contact-row">
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07A19.5 19.5 0 0 1 4.69 12 19.79 19.79 0 0 1 1.61 3.18 2 2 0 0 1 3.6 1h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L7.91 8.61A16 16 0 0 0 15.39 16.09l.96-.96a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
            <p class="footer-text">{officeInfo.phone}</p>
          </div>
          <div class="footer-contact-row">
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect width="20" height="16" x="2" y="4" rx="2"/><path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/></svg>
            <p class="footer-text">{officeInfo.email}</p>
          </div>
          <div class="footer-contact-row">
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect width="20" height="16" x="2" y="4" rx="2"/><path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/></svg>
            <p class="footer-text">{officeInfo.email2}</p>
          </div>
        </div>
      </div>

      <!-- Factory -->
      <div class="footer-col">
        <h3 class="footer-heading">Factory</h3>
        <div class="footer-contact-list">
          <div class="footer-contact-block">
            <p class="footer-label">Factory</p>
            <p class="footer-text">{factoryInfo.address}</p>
          </div>
          <div class="footer-contact-row">
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07A19.5 19.5 0 0 1 4.69 12 19.79 19.79 0 0 1 1.61 3.18 2 2 0 0 1 3.6 1h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L7.91 8.61A16 16 0 0 0 15.39 16.09l.96-.96a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
            <p class="footer-text">{factoryInfo.phone}</p>
          </div>
        </div>
      </div>

      <!-- Products Links -->
      <div class="footer-col">
        <h3 class="footer-heading">Products</h3>
        <ul class="footer-links">
          {products.map((product) => (
            <li><a href="#" class="footer-link">{product}</a></li>
          ))}
        </ul>
      </div>

      <!-- Company Links -->
      <div class="footer-col">
        <h3 class="footer-heading">Company</h3>
        <ul class="footer-links">
          {company.map((item) => (
            <li><a href="#" class="footer-link">{item}</a></li>
          ))}
        </ul>
      </div>

    </div>
  </div>
</footer>

<style>
  /* ── Shell ─────────────────────────────────────────────────────────────────── */
  .footer {
    background-color: white;
  }

  .footer-bar {
    height: 0.625rem;
    background: linear-gradient(to right, var(--primary-700), var(--primary-100));
  }

  .footer-inner {
    max-width: 90rem;
    margin: 0 auto;
    padding: 3rem 1.5rem;
  }

  .footer-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2.5rem;
  }

  /* ── Brand Column ───────────────────────────────────────────────────────────── */
  .footer-brand {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .footer-logo {
    width: 8rem;
    height: auto;
    object-fit: contain;
  }

  .footer-copy {
    font-family: var(--font-hanken);
    font-size: 0.75rem;
    font-weight: 400;
    color: var(--black);
    line-height: 1.5;
  }

  /* ── Content Columns ───────────────────────────────────────────────────────── */
  .footer-col {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .footer-heading {
    font-family: var(--font-hanken);
    font-size: 1.125rem;
    font-weight: 700;
    color: var(--primary-500);
  }

  /* ── Contact Blocks ─────────────────────────────────────────────────────────── */
  .footer-contact-list {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .footer-contact-block {
    display: flex;
    flex-direction: column;
    gap: 0.375rem;
  }

  .footer-contact-row {
    display: flex;
    align-items: flex-start;
    gap: 0.5rem;
    color: var(--black);
  }

  .footer-contact-row svg {
    flex-shrink: 0;
    margin-top: 0.2rem;
    color: var(--primary-500);
  }

  .footer-label {
    font-family: var(--font-hanken);
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--black);
  }

  .footer-text {
    font-family: var(--font-hanken);
    font-size: 0.875rem;
    font-weight: 400;
    color: var(--black);
    line-height: 1.6;
  }

  /* ── Nav Links ──────────────────────────────────────────────────────────────── */
  .footer-links {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .footer-link {
    font-family: var(--font-hanken);
    font-size: 0.875rem;
    font-weight: 400;
    color: var(--black);
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .footer-link:hover {
    color: var(--primary-500);
  }

  /* ── Responsive ─────────────────────────────────────────────────────────────── */
  @media only screen and (min-width: 768px) {
    .footer-inner {
      padding: 4rem 3rem;
    }

    .footer-grid {
      grid-template-columns: 1fr 1.6fr 1fr 1fr 1fr;
      gap: 3rem;
    }

    .footer-logo {
      width: 9rem;
    }
  }

  @media only screen and (min-width: 1024px) {
    .footer-inner {
      padding: 5rem 6.25rem;
    }

    .footer-logo {
      width: 10rem;
    }
  }
</style>
